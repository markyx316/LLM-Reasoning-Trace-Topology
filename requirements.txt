# =============================================================================
# Reasoning Trace Topology UQ - Requirements
# =============================================================================
# Install: pip install -r requirements.txt
# For GPU inference, install PyTorch separately with CUDA support first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# --- Core ML & NLP ---
transformers>=4.45.0
accelerate>=0.34.0
datasets>=3.0.0
tokenizers>=0.20.0
sentencepiece>=0.2.0
safetensors>=0.4.0

# --- Quantization (optional, for 4-bit inference) ---
bitsandbytes>=0.44.0

# --- Fast inference (optional, recommended) ---
# vllm>=0.6.0  # Install separately: pip install vllm

# --- Sentence embeddings ---
sentence-transformers>=3.0.0

# --- NLP processing ---
spacy>=3.7.0
# After install: python -m spacy download en_core_web_sm
nltk>=3.9.0
regex>=2024.0.0

# --- Classical ML ---
scikit-learn>=1.5.0
xgboost>=2.1.0
shap>=0.45.0

# --- Data & computation ---
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0

# --- Visualization ---
matplotlib>=3.9.0
seaborn>=0.13.0

# --- NLI model for semantic entropy baseline ---
# (uses transformers pipeline, no extra install)

# --- Experiment tracking ---
pyyaml>=6.0.0
tqdm>=4.66.0

# --- Symbolic math (for answer equivalence checking) ---
sympy>=1.13.0

# --- Reproducibility ---
# Record: pip freeze > requirements_frozen.txt
