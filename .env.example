# Copy this to .env and fill in your keys

# HuggingFace token (needed for gated models / higher rate limits)
HF_TOKEN=hf_...

# DeepSeek API key (only needed for --mode deepseek-api)
DEEPSEEK_API_KEY=sk-...
